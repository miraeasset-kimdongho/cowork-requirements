temp.md

# Mattermost 자동화 - 비밀정보(토큰/웹훅) 절대 커밋 금지
scripts/mm-config.local.json
*.local.json
.mm-state

# 환경변수 비밀 파일 (절대 커밋 금지)
.env
.env.*
!.env.example

# 분석용으로 클론해둔 외부 레포들 — 이 레포에 중복 추적 금지
codes/

# 분석 산출물 — 커밋하지 않음(이 레포는 PRD/이슈 문서 저장소)
review.md

# 자원예약 API 파일럿/스크래치 — 실세션 토큰(memo.md)·사번·내부 엔드포인트 포함, 커밋 금지
pilot/
